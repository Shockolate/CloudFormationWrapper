source 'https://rubygems.org' do
  gem 'aws-sdk-cloudformation'
  gem 'rake', :require => false, :group => :test
  gem 'rubocop', :require => false, :group => :test
  gem 'yard', :require => false, :group => :test
  gem 'simplecov', :require => false, :group => :test
  gem 'codeclimate-test-reporter', '~> 1', :require => false, :group => :test
  gem 'gem-release', :require => false, :group => :test
end